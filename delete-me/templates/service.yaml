apiVersion: v1
kind: Service
metadata:
  name: {{ include "delete-me.fullname" . }}
  labels:
    {{- include "delete-me.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "delete-me.selectorLabels" . | nindent 4 }}
