apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "delete-me.fullname" . }}-test-connection"
  labels:
    {{- include "delete-me.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "delete-me.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
